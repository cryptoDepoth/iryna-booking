# Iryna Booking System — Агент-координация

## 🗂️ Источник правды (Source of Truth)
**Только одна папка:** `/Users/andrzej/business/iryna/iryna-booking/`

Корневая `./iryna-booking/` — устаревшая копия, НЕ использовать для деплоя.

## 🔄 Процесс: Локально → GitHub → Fly.io

```
Локальная папка → git commit → git push → fly deploy ✈️
                                        ↑
                                   GitHub repo
```

**НЕ деплоить из `./iryna-booking/` — деплоить только из `./business/iryna/iryna-booking/`**

## 📝 Журнал изменений (последний агент записывает)

| Дата | Агент | Модель | Что сделано | Проверено |
|------|-------|--------|------------|-----------|
| 2026-05-09 | Hermes | kimi-k2.6 | Исправил events.yaml на сервере (lilac-jun7, blossom-may3) | ✅ API /events, /slots работают |
| | | | ОБНАРУЖЕН БАГ: /book и /admin/stats дают 404 (деплоен урезанный код) | ⚠️ Требуется передеплой из правильной папки |

## ✅ Чеклист перед деплоем

- [ ] Проверить `pwd` — must be `/Users/andrzej/business/iryna/iryna-booking/`
- [ ] Запустить тесты: `python -m pytest tests/`
- [ ] Проверить events.yaml содержит нужные события
- [ ] `git status` — все изменения закоммичены
- [ ] `git push`
- [ ] `fly deploy` (из правильной папки!)
- [ ] Проверить на проде: /events, /slots, /book, /admin/stats

## 🚨 Edge-кейсы (не ломать!)

1. **events.yaml** — единственный источник событий. При создании event через админку — обязательно проверить, что другие events не удалились.
2. **Database** `/data/bookings.db` — persist на Fly volume, не дропать.
3. **E-Transfer** — `processed_emails.json` лежит в `/data/`, тоже persist.

## 📞 Контекст для передачи другому агенту

> "Работай в `/Users/andrzej/business/iryna/iryna-booking/`. Корневая `./iryna-booking/` — устаревшая копия. Перед изменением — запусти `python -m pytest tests/`. После деплоя — проверь GET /events, GET /slots/2026-06-07?event_id=lilac-jun7, GET /book — все должны вернуть 200."
