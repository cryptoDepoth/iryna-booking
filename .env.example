# Notion Integration
NOTION_API_KEY="your_notion_api_key_here"
NOTION_DATABASE_ID="your_database_id_here"

# Admin API Key (for /admin, /admin/confirm, /admin/cancel)
ADMIN_KEY="your_secure_admin_password"

# Flask secret key (stable across restarts)
FLASK_SECRET_KEY="change-me-to-a-random-string"

# Telegram Notifications (for photographer)
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"
TELEGRAM_CHAT_ID="your_telegram_chat_id"

# Google Calendar (optional)
GCAL_HELPER=""
BOOKING_CALENDAR_ID="iryna.pashynska@gmail.com"
BOOKING_CALENDAR_TZ="America/Edmonton"

# Stripe (card payments — optional, Interac e-Transfer works without this)
# Get keys from: https://dashboard.stripe.com/apikeys
# Get webhook secret from: https://dashboard.stripe.com/webhooks
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Meta Ads — Pixel + Conversions API (analytics / ad optimization)
# META_PIXEL_ID: the SINGLE pixel id for the whole site. Must match the pixel your
#   ad account optimizes against (Events Manager). Defaults to the canonical pixel
#   in app.py; set here only to switch pixels. Injected into every template.
META_PIXEL_ID="1335137335347797"
# META_CAPI_TOKEN: enables server-side Purchase (fires when an e-Transfer is
#   confirmed async, after the client has left). No-op until set. Create a
#   system-user token in Events Manager → Settings → Conversions API.
META_CAPI_TOKEN=""
# META_TEST_EVENT_CODE: set temporarily to verify events in Events Manager →
#   Test Events, then clear it. META_CAPI_API_VERSION defaults to v19.0.
META_TEST_EVENT_CODE=""

# Website AI assistant (optional)
# Build sanitized knowledge from the Instagram export:
# python3 scripts/build_assistant_knowledge.py /path/to/instagram-export.zip
AI_PROVIDER="zai"
ZAI_API_KEY=""
ZAI_MODEL="glm-4.5-air"
ZAI_THINKING="disabled"
ZAI_CHAT_COMPLETIONS_URL="https://api.z.ai/api/paas/v4/chat/completions"
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4.1-mini"
ASSISTANT_KNOWLEDGE_PATH="/data/assistant_knowledge.jsonl"
ASSISTANT_MAX_KNOWLEDGE="10000"
ASSISTANT_SITE_URL="https://pashynska.agency"

# Automatic Google review request safety gates. A review is sent only after
# both windows have passed and a gallery delivery was recorded in Admin.
REVIEW_REQUEST_MIN_SESSION_DAYS="14"
REVIEW_REQUEST_GALLERY_DELAY_DAYS="3"
