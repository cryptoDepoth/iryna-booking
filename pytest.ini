[pytest]
# Tests do `import app as booking_app`, which requires the repo root on
# sys.path. Setting pythonpath here means `pytest tests/ test_admin.py`
# works without `PYTHONPATH=.` on every invocation.
pythonpath = .
testpaths = tests test_admin.py
